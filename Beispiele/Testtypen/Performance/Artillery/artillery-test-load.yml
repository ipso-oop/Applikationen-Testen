config:
  target: "http://localhost:3000"
  phases:
    - duration: 60          # 1 Minute
      arrivalRate: 10       # 10 Benutzer pro Sekunde
scenarios:
  - flow:
      - get:
          url: "/api/slow"

